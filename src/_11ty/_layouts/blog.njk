---
title: Blog
description: In questa pagina troverai tutti i pots che ho pubblicato
keyword: test
type: website
---
<!DOCTYPE html>
<html lang="it">
{% include "head-website.njk" %}
<!-- Start Body -->
<body>
    {% include "nav.njk" %}
    <main>
        <ul class="elenco-posts">
            <li class="posts">
                {{content | safe}}
            </li>
        {%- for post in collections.post | reverse -%}
            <li class="posts">
                <h2><a href="{{post.url}}">{{post.data.title}}</a></h2>
                <p>{{post.data.description | safe}}</p>
                <small class="info">
                    <time datetime="{{post.data.date}}">{{post.data.date | readableDate}}</time>&nbsp;&nbsp;&ndash;&nbsp;
                    {{post | readingTime}}&nbsp;&nbsp;&ndash;&nbsp;
                    {{post.data.tags}}
                </small>
            </li>
        {%- endfor -%}
        </ul>
    </main>
    {% include "footer.njk" %}
</body>
<!-- End Body -->
</html>
